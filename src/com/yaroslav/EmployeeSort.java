package com.yaroslav;

import java.time.LocalDate;
import java.util.HashMap;

public class EmployeeSort {
    // Sorts employees by birthdate
    // employees: employees array
    // monthCount: 0 - for current month, 1 - for current and next month, etc..
    public static HashMap<LocalDate, Employee> sortByBirthdate(Employee[] employees, Integer monthCount) {
        HashMap<LocalDate, Employee> map = new HashMap<>();

        return map;
    }
}
